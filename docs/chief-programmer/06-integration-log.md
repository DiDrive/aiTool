# 集成记录

## 2026-08-06

- 选择 `tigerowo/infinite-canvas` 作为独立 sidecar，而不是复制其 React 页面。
- 新增 `infiniteCanvas` MAPI：配置、状态、启动、停止、打开。
- 新增 `/canvas` 页面和侧边栏入口。
- 本地模式约定 Go API + Next standalone；external 模式可连接已有部署。
- 设置安全独立窗口、同源导航限制和权限拒绝。
- 新增 Docker 多阶段 Windows sidecar 构建脚本；产物包含修改后的完整对应源码。
- 本次锁定检查的上游版本为 `v0.5.1`，commit `d6ec27a1fd47a53560671b1a7a99b1e0139135e1`。
- Docker Hub 直连失败后，使用显式 DaoCloud 镜像前缀完成构建；构建脚本保留默认
  Docker Hub，并允许通过 `-DockerImagePrefix` 显式选择镜像仓库。
- 已生成 `v0.5.1+workbench.1` Windows sidecar：2080 个文件，共 167,281,070 字节。
- `server.exe` SHA-256：`E07C99B925281C5C7E6D8250F3C7A865A65E31049D7D12C3E76D99428C3AA6F7`。
- `web/server.js` SHA-256：`5488CB66B6CA051A478B42267D1CED2B6779B99F77721BA6714AC9AC866D1F6F`。
- Windows Go API 与 Next `/api/health` 代理均通过真实进程 smoke test，返回 `HTTP 200 ok`。
- 工作台画布页简化为单一“打开画布”按钮。
- 本地模式会自动创建/登录专用 `workbench` 用户并直接进入 `/canvas`，不再暴露登录步骤。
- 每次打开时把工作台“直连 API 平台”的可用渠道同步到画布用户配置；认证与配置同步
  已通过真实 API smoke test。
- 本地运行包尚未纳入仓库；在完成 fork、回环监听补丁和 AGPL 发布材料前，不能标记
  为可发布状态。

## 2026-08-07

- 确认上游画布已包含剧本捕获、镜头拆解、角色四视图、分镜拼图及视频生成 Agent，
  无需新增平行节点系统。
- 工作台打开画布时新增文本模型渠道同步：读取“模型设置”中已启用的文本模型，写入
  画布的 `textModels`、`textModel` 和 `textChannelId`。
- 文本、图片、视频渠道保持独立；文本走通用 OpenAI 兼容 `/v1/chat/completions`，媒体
  继续走各平台协议适配。
- `npx vite build` 构建通过。真实全链路生成仍需使用用户已配置且支持 Tool Calling 或
  稳定 JSON 输出的文本模型验收。
- 增加 `workflowKind` 可选元数据，不改变上游 Text/Image/Video 节点类型和存储格式；
  旧画布可继续读取。
- 画布空白处创建菜单及拖线末端菜单新增创意、剧本、分镜脚本、人物素材、分镜图和
  视频片段；节点悬浮工具栏可直接运行当前节点。
- 右侧 Agent 上下文可读取创作角色，Agent 创建的剧本及按标题推断出的分镜、人物、
  视频节点写回同一 `workflowKind`，与手动节点共用连线和执行引擎。
- 已完成 `v0.5.1+workbench.5` Next 生产构建并安装到工作台运行包。真实模型端到端生成
  仍需在工作台重启后验收。

## 2026-08-10

- 根据实际界面验收修正创作节点交互：底部工具栏新增创意、剧本、分镜脚本、人物素材、
  分镜图、视频片段六个带文字的快捷按钮。
- 画布空白处右键现在直接打开节点创建菜单；保留双击和拖线末端创建方式。
- 空创作节点以“AI 生成”为主操作，同时提供“生成要求”入口；创建节点后自动展开模型、
  提示词和媒体参数面板。AI 生成结果仍可人工编辑修订。
- Docker Hub 连接失败后使用 `m.daocloud.io/docker.io` 镜像前缀完成 Next 生产构建；
  `v0.5.1+workbench.6` 已安装到工作台运行包，构建产物中已检出新增中文入口。
- 仍需重启当前工作台/sidecar 后进行真实 UI 点击和模型调用验收。
- 新增结构化场次 `scriptScenes`、镜头 `storyboardShots` 与逐镜头 `assetNodeIds` 数据契约；
  旧纯文本剧本会降级解析为可编辑表格。
- 双击剧本打开全屏场次表，可直接生成并连线结构化分镜脚本；双击分镜脚本打开镜头表、
  素材绑定和完整度检查中心。
- 支持逐镜头/批量生成缺失人物、场景、道具与分镜图，也可把画布中上传的图片绑定到
  对应素材槽位。视频按钮和执行函数都对素材完整度做门禁。
- Agent 上下文包含结构化场次和镜头；图片/视频工具新增镜头及素材角色参数，执行后将
  节点 ID 回写镜头绑定，Agent 视频工具同样执行缺失素材检查。
- `v0.5.1+workbench.7` 已通过 Next 生产构建和独立 `tsc --noEmit`，最终运行包已安装。
- 修复模型返回 Markdown 时被错误降级为单场剧本或逐行分镜的问题：未通过结构校验的
  内容改为完整原文模式，不再填入不匹配的表格列。
- 新增“AI 结构化原文”；新生成剧本/分镜首次输出不合格时自动进行一次保剧情结构修复。
  场次要求场景、剧情/对白和时长，镜头要求景别、场景、画面、运镜和时长等关键字段。
- 结构化提示词进一步约束目标时长、字段对应关系和禁止教程式回答；长文本单元格按内容
  增高，减少剧情被 textarea 折叠的问题。
- `v0.5.1+workbench.8` 已通过 `tsc --noEmit`、Next 生产构建和补丁双向检查，并安装。
- 剧本、分镜脚本、素材描述与 Agent 回复新增简体中文强制契约；英文结构化输出也视为
  不合格，并由已有的一次结构修复调用完成翻译和字段重组。
- 中文校验覆盖各个已填写的剧情/分镜字段，避免仅凭单个中文字符误判整段英文为合格；
  自定义提示词即使自带 JSON schema，也会在缺少语言约束时追加简体中文契约。
- `v0.5.1+workbench.9` 已通过独立 `tsc --noEmit`、Next 生产构建、补丁正反向检查，
  并安装到工作台本地运行包；构建产物已检出中文契约文本。
- 详情弹窗不再信任旧版本保存的 `workflowStructureValid=true`，会按当前中文字段契约重新
  校验已有场次/镜头；旧英文节点保留原文并开放“AI 结构化原文”完成翻译。
- 分镜素材绑定由原生下拉框改为卡片：显示真实缩略图、画布节点名称和生成状态，图片可
  放大预览；“选择/更换”打开当前画布图片的可视化选择器，“解绑”只解除关系。
- 每个镜头可手动增加人物、场景、道具或其他参考素材位。`extraAssets` 保存显示名称、类型
  和真实节点 ID，并纳入缺失素材提示、视频门禁、分镜图与视频的依赖连线。
- 已有分镜图/视频节点重新生成时会补充当前素材依赖连线，避免后绑定素材只显示但不参与
  生成。`v0.5.1+workbench.10` 已通过 `tsc --noEmit`、Next 生产构建和补丁双向检查，
  并安装到工作台本地运行包；构建产物已检出“手动增加素材”入口。
- 修复素材绑定只改 `storyboardShots` 而不更新连线的问题。现在所有基础和额外素材变更都
  通过统一同步器比较前后绑定，只移除对应分镜图/视频节点的旧受管依赖并补上新依赖。
- 修复分镜图按钮的静默路径：绑定 ID 指向已删除节点时新建分镜图节点，指向空节点时重试，
  指向已有成图时创建新版本并更新绑定，旧图保留在画布但不再作为当前受管依赖。
- `v0.5.1+workbench.11` 已通过独立 `tsc --noEmit`、Next 生产构建和补丁双向检查，
  并安装到工作台本地运行包。
- 增加旧数据自愈：打开分镜详情时会用当前素材绑定重新同步受管依赖；对已知分镜图和视频
  目标，未出现在当前绑定中的历史图片输入线会被移除，非图片配置等其他连线保留。
- `v0.5.1+workbench.12` 已通过最终 `tsc --noEmit`、Next 生产构建和补丁双向检查，
  并安装到工作台本地运行包。
- 根据用户本机 `2026-08-10` AI 调用日志确认：普通图片成功请求 PIX `gpt-image-2`
  `/images/generations`；分镜图请求 `/images/edits` 并携带 7–8 张素材，先后返回
  `insufficient_funds` 与 `all_vendors_failed/upstream_outage`，并非画布按钮未提交任务。
- 人物与道具数组现在在解析和打开旧分镜时按规范化名称去重；AI 提取的基础人物、场景、
  道具卡片新增逐项“移除”，同时更新绑定和受管连线但保留画布素材节点。
- 分镜图多图编辑最多提交四张关键参考；已知编辑渠道错误时自动改用同模型文生图，界面明确
  提醒此次降级且保留全部素材绑定，便于更换渠道后重新生成。
- `v0.5.1+workbench.13` 已通过 `tsc --noEmit`、Next 生产构建和补丁双向检查，并安装；
  构建产物已检出多图编辑降级提示。

## 2026-08-10（分镜多图协议根因修复）

- 对照工作台普通生图实现与服务日志确认：PIX GPT Image 2 支持最多 16 张参考图；失败并非
  Image2 的 4 张限制，而是画布将多张参考图错误转换成了 `mode=image-edit`。
- 修复 PIX 请求转换：单图为 `image-edit`，多图为 `multi-reference`；画布恢复提交全部绑定
  素材，删除 4 张裁剪和纯文本自动降级。
- 超过 PIX 模型真实参考图上限时返回包含模型、上限与当前数量的明确错误。
- Go handler 测试新增多图模式与 17 张拒绝用例；`go test ./handler` 与前端 `tsc --noEmit`
  均通过。
- `v0.5.1+workbench.14` 已通过 Next 生产构建和补丁正反向检查并安装。因旧 sidecar 进程
  仍占用 `server.exe`，本机 manifest 暂指向并行安装的 `api/server-workbench14.exe`；工作台
  完全退出后重新打开即启用新版服务端。

## 2026-08-10（本地模型渠道匹配修复）

- 服务日志确认 `gpt-5.6-sol`、`gpt-5.6-luna`、`doubao-seed-2-0-pro` 在切换后仍使用固定
  `textChannelId`，服务端因此在渠道白名单校验阶段返回“本地渠道不支持该模型”。
- `channelIdForActiveModel` 现在先验证首选渠道是否声明当前模型，不匹配时从同步的本地渠道
  中选择真正包含该模型的渠道；前端 `tsc --noEmit` 已通过。
- `v0.5.1+workbench.15` 已通过 Next 生产构建和补丁正反向检查并安装；本机 manifest 指向
  并行安装的 `api/server-workbench15.exe`，完全退出工作台后重启生效。

## 2026-08-10（视频倒推剧情与剧本）

- 确认原节点上下文虽然收集了 `referenceVideos`，但 `buildNodeChatMessages` 只提交文字和图片，
  视频 URL 从未进入模型可理解的内容，因此模型只能根据标题和通用模式猜测剧情。
- 新增浏览器端视频时间线分析：解析上传视频，按时长抽取 12～20 张、总计最多 20 张 JPEG
  关键帧，并按时间顺序追加到文本模型多模态请求。
- 倒推契约要求严格还原可见人物、场景、动作、镜头、情绪和字幕；未转写的声音不得虚构
  对白。关键帧加载、解码、寻址或画布绘制失败时直接显示错误并停止生成。
- 主生成和重试链路都启用视频分析；前端两次 `tsc --noEmit` 均通过。
- `v0.5.1+workbench.16` 已通过 Next 生产构建和补丁正反向检查并安装；本机 manifest 指向
  `api/server-workbench16.exe`，完全退出工作台后重启生效。

## 2026-08-10：倒推剧情的 AI 改编

- 剧本详情新增“AI 改编”，用户可输入人物、背景、情节、风格、结局和时长等改编要求。
- 改编不覆盖倒推原稿，而是创建与原稿连线的独立“改编剧本”节点；新节点仍输出完整中文
  结构化剧本，可继续生成分镜脚本。
- 改编请求只以原剧本正文和用户要求为输入，不重复触发上游视频忠实倒推约束。
- `v0.5.1+workbench.17` 已通过独立 TypeScript 检查、Next 生产构建和补丁正反向检查，
  并安装为 `api/server-workbench17.exe`。

## 2026-08-10：视频任务完成回写与单镜头提示词

- 从实际 SQLite 任务记录确认，异常任务 `client_video_task_video-1786356873749-o4q24` 已有上游
  task ID，但状态保存为中文“生成中”；轮询器只扫描英文活动状态，导致任务永远停在 0%。
- 服务端现统一识别中文排队、生成中、完成、失败状态，并把历史中文活动状态加入恢复扫描。
- 单镜头视频节点不再自动拼接整份分镜脚本文本；每次运行会按所选镜头重建提示词，仅保留
  当前镜头绑定的图片素材作为参考。
- `v0.5.1+workbench.18` 已通过 Go 单元测试、TypeScript 检查、Next 生产构建和补丁双向检查，
  并安装为 `api/server-workbench18.exe`。

## 2026-08-12：视频裁剪、精细倒推与梗概改编第一阶段

- 视频节点双击新增非破坏裁剪和独立倒推分析范围；范围使用原视频绝对毫秒，可选择快速、
  标准或精细模式，节点播放遵守裁剪范围且原视频文件不改变。
- 旧的全片 12～20 帧均匀采样改为分析范围内 2/1/0.5 秒候选采样，并将每 9 张带绝对
  时间码的证据帧组成联系表。最多 20 张模型图片现在可承载最多 180 个连续证据瞬间，且
  每次强制保留分析范围首尾。
- 倒推提示词明确要求逐联系表、逐时间片检查，输出原视频绝对 `startMs/endMs`，并提取
  稳定人物、场景、服装、色彩、灯光和镜头语言一致性信息。
- 剧本/分镜数据契约增加绝对起止时间、人物/场景稳定 ID、证据时间码、置信度和视觉圣经；
  旧数据字段保持可选，继续兼容已有画布。
- 剧本 AI 改编支持粘贴或上传 TXT/Markdown 梗概，以及结构复刻、节奏复刻、剧情优先三种
  模式。改编继续创建独立连线节点，不覆盖原片倒推分支。
- 当前阶段尚未接入 ASR、说话人分离、OCR 和 Agent 新工具；这些能力保留为 VA-05/VA-06。
- 当前完整源码已通过 Docker 内 `bun run build` 的 Next 生产构建与 Windows Go 编译。
# 2026-08-12 专业时间线编辑器

- 视频节点双击编辑升级为监视器、运输控制、缩略图、标尺、播放头、裁剪轨和分析轨组合界面。
- 支持逐帧、缩放、点击定位及 Space、方向键、I/O 快捷键，仍保持非破坏编辑。
- `bun x tsc --noEmit`、Next.js 生产构建和 Windows Go sidecar 构建通过。
- 本地运行清单切换到 `v0.5.1+workbench.21`。
- 分析阶段增加低分辨率亮度差异扫描，优先保留切镜/显著变化候选点，再用固定时间采样补齐。
- 镜头变化版本通过严格类型检查与生产构建，并切换到 `v0.5.1+workbench.22`。
- Agent 增加 `set_video_analysis_range` 与 `reverse_video_to_script`，可真实修改视频元数据并调用同一关键帧倒推链路。
- Agent 版本通过严格类型检查与生产构建，并切换到 `v0.5.1+workbench.23`。

# 2026-08-12 视频比例与时间显示修复

- 监视器从固定 16:9 改为读取 `videoWidth/videoHeight`，横屏占宽、竖屏占高，保持原比例完整显示。
- 裁剪编辑器、时间标尺、提示和剧本/分镜详情统一使用秒；数据层继续使用毫秒保证逐帧精度与兼容性。
- 严格类型检查、Next.js 生产构建和 sidecar 构建通过，本地运行包切换到 `v0.5.1+workbench.24`。

# 2026-08-12 一致性设定与素材风格继承

- 剧本和分镜详情增加视觉总规范、人物一致性、场景一致性的可编辑面板。
- 剧本生成分镜时复制一致性档案；旧分镜可沿来源节点链继承档案并按分支覆盖。
- 人物/场景/道具素材、分镜图和视频生成提示词按镜头自动注入对应档案。
- 严格类型检查和生产构建通过，本地运行包切换到 `v0.5.1+workbench.25`。

# 2026-08-12 倒推一致性档案回填

- 修复结构修复契约只要求 scenes/shots，导致顶层一致性档案丢失的问题。
- 分镜脚本解析器现在接收并保存 characters、locations、visualBible。
- 已有空数据从场次/镜头中的人物、场景、画面、光线、运镜和平均镜头时长自动推导基础档案。
- 严格类型检查与生产构建通过，本地运行包切换到 `v0.5.1+workbench.26`。

# 2026-08-12 具体画风分析与生成门禁

- 泛化的“依据原视频/保持原风格”等回填不再视为有效风格。
- 详情增加“AI 分析视频风格”，沿工作流找到上游视频并使用关键帧单独提取具体媒介、地域画风、色彩、渲染、人物和场景档案。
- 风格档案未具体化前禁用人物素材、分镜图和视频生成，避免 3D 动画错误退化为真人摄影。
- 严格类型检查与生产构建通过，本地运行包切换到 `v0.5.1+workbench.27`。

# 2026-08-12 视频风格分析进度反馈

- “AI 分析/重新分析视频风格”接入真实异步生命周期，分析期间按钮显示加载状态并禁止重复提交。
- 详情页显示关键帧提取、画面证据整理、AI 风格识别、结果校验与写回等阶段和动态进度。
- 只有返回足够具体的视觉规范才视为成功；失败会结束进度并保留重试入口。
- 严格类型检查与生产构建通过，本地运行包切换到 `v0.5.1+workbench.28`。

# 2026-08-12 空素材语义过滤

- 分镜结构化时将“无、没有、暂无、无需、无道具、none、n/a”等空语义转换为空素材集合。
- 打开已有分镜详情时自动清理这类历史素材及对应绑定，避免空素材卡片和隐藏连线残留。
- 额外素材和手动编辑入口使用同一过滤规则；“无人机”等真实素材名称不受影响。
- 严格类型检查与生产构建通过，本地运行包切换到 `v0.5.1+workbench.29`。

# 2026-08-12 相邻视频片段连续性约束

- 工作流视频片段自动将本镜头分镜图绑定为首帧，不再只作为普通参考图。
- 同场景且人物连续的相邻镜头共享边界分镜图：前段尾帧与后段首帧使用同一视觉锚点。
- 视频提示词注入上一镜结束状态、本镜头开始状态和下一镜目标状态，约束人物站位、动作、道具、环境和光线连续。
- 场景切换不错误复用尾帧，明确要求当前片段保持自身场景稳定。
- 连线同步覆盖首尾帧节点，严格类型检查与生产构建通过，运行包切换到 `v0.5.1+workbench.30`。

# 2026-08-13 分镜绑定恢复与真实视频裁剪

- 打开分镜详情时按来源分镜、镜头 ID、素材类型和素材名称自动恢复已有素材/分镜图/视频绑定，不触发重新生成。
- 新建工作流素材写入稳定的 `workflowAssetKind` 与 `workflowAssetName`，减少节点标题或结构化结果变化导致的绑定丢失。
- 视频弹窗明确区分“输出片段”和“AI 分析片段”，底部提供“仅保存范围”与“生成裁剪视频”。
- 工作台主进程增加仅回环地址、随机令牌、仅接收视频字节的 FFmpeg 裁剪桥；生成结果作为新视频节点加入画布，原视频保持不变。
- 无限画布严格类型检查、Next.js 生产构建及工作台 Vite/Electron 生产构建通过，运行包切换到 `v0.5.1+workbench.31`。

# 2026-08-13 工作流素材提示词隔离

- 修复人物、场景和道具参考图错误继承整份分镜脚本的问题；工作流素材节点统一启用 `excludeUpstreamText`，不再把上游所有镜头文本追加到生成请求。
- 单素材提示词不再包含创建时所在镜头的画面、动作、对白和其他角色；人物仅保留人物档案与素材视觉规范，场景仅保留场景档案与素材视觉规范，道具仅保留道具自身约束与素材视觉规范。
- 再次执行“补全素材”时会同步刷新已有素材节点的隔离提示词；已完成图片不会被自动重新生成，用户主动重试时才使用新提示词。
- 严格 TypeScript 检查与 Next.js 生产构建通过，运行包切换到 `v0.5.1+workbench.32`。

# 2026-08-14 视频裁剪与 AI 分析编辑器拆分

- 视频编辑弹窗拆分为“视频裁剪”和“AI 分析片段”两个独立页签，分别提供裁剪输出与分析范围保存操作，不再把两种任务混在同一面板。
- 删除快速/标准/精细选项，浏览器取帧、旧数据和 Agent 设置统一使用精细分析：约 0.5 秒候选采样并以 0.25 秒扫描镜头变化。
- 裁剪与分析范围改为相互独立的原视频绝对时间区间；生成新裁剪视频后，新节点继续使用自身 0 秒起算的完整分析范围。
- 时间线范围把手直接覆盖缩略帧轨；拖动入点或出点时，范围外帧实时变暗、选中区边框和播放器定位同步变化。
- 严格 TypeScript 检查与 Next.js 生产构建通过，运行包切换到 `v0.5.1+workbench.33`。
